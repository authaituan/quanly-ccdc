# PROJECT STATUS — Quản lý CCDC (IT Asset Management)

> AI-readable progress snapshot. Mọi dòng trong bảng dưới đây được audit trực
> tiếp từ source code thật tại thời điểm `audit_date`, không suy luận từ tên
> file/tên module. Khi code thay đổi, file này phải được cập nhật lại bằng
> cách audit lại — không copy/paste phiên bản cũ.

## META

```yaml
repo: authaituan/quanly-ccdc
branch: feature/scaffold-phase1
commit_hash: e70726a12704fc283887cdf18fd740fa72a712f5
commit_date: 2026-08-11T09:47:07+07:00
audit_date: 2026-08-11T15:40:00+07:00
auditor: Claude (Technical Auditor role, per project operating constitution)
```

## Nguồn sự thật đã đối chiếu

| Ưu tiên | Nguồn | Trạng thái tại thời điểm audit |
|---|---|---|
| 1. Runtime | `npm run start:dev` (backend) + `npm run dev` (frontend) đã chạy thật và được verify qua nhiều phiên liên tiếp (FE-02, AST-04/05, AUTH-01/02, FE-03, CRED-02/03/04) bằng curl và browser thật — **có bằng chứng runtime**. | Đã audit thật — xem bảng bên dưới |
| 2. Database | PostgreSQL 17 local (`postgresql-x64-17` service), DB `quanly_ccdc` | Đã audit thật — xem bảng bên dưới |
| 3. Source code GitHub | Nhánh `feature/scaffold-phase1`, commit `570f363` | Đã audit thật — xem bảng bên dưới |
| 4. Commit history | Xem `git log` trực tiếp để có danh sách đầy đủ, chính xác tại thời điểm đọc — không liệt kê tay từng commit ở đây nữa (dễ lỗi thời, đã từng gây mâu thuẫn thật trong file này) | Đã đọc |
| 5. Prompt mô tả | Chat trước đó | Thấp nhất, không dùng làm evidence |

## Bảng tiến độ

| Hạng mục | Trạng thái | Evidence (file:dòng) |
|---|---|---|
| Data model Prisma đầy đủ (18 model/enum) | ✅ Có, đã migrate | `backend/prisma/schema.prisma` — 12 model + 6 enum: `AssetCategoryGroup`(22), `AssetCategory`(30), `Site`(50, +7 field mới - xem OPS-02 bên dưới), `RackCabinet`(71), `RackPlacement`(86), `OperatingStatus`(105), `OwnershipStatus`(113), `ItAsset`(119, +`currentUser`), `Department`(157), `Employee`(170), `AssignmentStatus`(189), `EndUserAssignment`(195), `MaintenanceType`(221), `MaintenanceLog`(229), `SoftwareLicenseLink`(249), `NetworkAccessCredential`(269), `UserRole`(287), `User`(294) |
| `Site` — 7 field mới (Book1.xlsx import) | ✅ Đã migrate + có dữ liệu thật | `backend/prisma/schema.prisma:60-67`: `provinceCode`, `provinceName`, `regionCode`, `wardCode`, `wardName`, `centralWardName`, `pointType` (tất cả `String?`). Migration `backend/prisma/migrations/20260810094730_add_book1_import_fields/migration.sql`. Xác nhận `\d sites` thật có đủ 7 cột. Mẫu dữ liệu thật: site `536750` → `provinceCode='53'`, `regionCode='5310'`, `pointType='GD3'` |
| `ItAsset.currentUser` | ✅ Đã migrate + có dữ liệu thật | `backend/prisma/schema.prisma:149`. Query thật: `SELECT count(*) FROM it_assets WHERE "currentUser" IS NOT NULL` → 99 |
| `AssetCategory.UNCLASSIFIED` | ✅ Đã seed | `backend/prisma/seed.ts` — group `END_USER`, dùng cho 73 dòng Book1.xlsx thiếu "Loại máy" hoặc ghi "Không có máy tính" |
| Migration áp dụng vào DB thật | ✅ Đã chạy (2 migration) | `backend/prisma/migrations/`: `20260810085140_init`, `20260810094730_add_book1_import_fields`. Re-verify `npx prisma migrate status` → "Database schema is up to date!" |
| Seed taxonomy | ✅ Đã chạy | `backend/prisma/seed.ts`; output thật "Seeded 29 asset categories" (28 gốc + `UNCLASSIFIED`). Query thật `SELECT count(*) FROM asset_categories` → 29 |
| **OPS-02 — Import Book1.xlsx (Merge1) vào DB** | ✅ **Đã hoàn thành** | Script: `backend/prisma/import-book1.ts` (dry-run mặc định, `--commit` để ghi thật). Kết quả thật (query `psql` độc lập, không chỉ dựa vào log script): `SELECT count(*) FROM sites` → **206**; `SELECT count(*) FROM it_assets` → **359**; theo category: `PC_DESKTOP`=286, `UNCLASSIFIED`=73; `serialNumber` distinct=non-null=255 (không trùng); `network_access_credentials` = 0 dòng (xem rủi ro #4 bên dưới). Commit `2ad8953`. |
| **AST-04 — `PATCH /assets/:id`** | ✅ **Đã hoàn thành**, verify runtime thật | `backend/src/modules/assets/assets.controller.ts` (`update()`), `assets.service.ts` (`update()` + `assertCategoryExists`/`assertSiteExists`/`assertAssetTagAvailable`/`assertSerialNumberAvailable`), `dto/update-asset.dto.ts` (`PartialType(CreateAssetDto)`, cần thêm dependency `@nestjs/mapped-types`). Cho sửa mọi field kể cả `assetTag`/`serialNumber` (unique), validate FK + unique thủ công trước khi ghi (quyết định 2026-08-10). Verify thật bằng curl vào server đang chạy: PATCH field hợp lệ → 200 + data đúng; PATCH `categoryId` không tồn tại → **400** `"categoryId ... không tồn tại"`; PATCH `assetTag` trùng → **409** `"assetTag ... đã tồn tại"`. |
| **AST-05 — `DELETE /assets/:id` (soft delete)** | ✅ **Đã hoàn thành**, verify runtime thật | `assets.service.ts` (`decommission()`) — **không xóa dòng khỏi `it_assets`**, chỉ chuyển `operatingStatus` sang `DECOMMISSIONED` (quyết định 2026-08-10: đây là hệ thống quản lý tài sản, không được mất lịch sử kiểm toán). Verify thật: DELETE 1 asset thật → `SELECT count(*) FROM it_assets` vẫn = **359** (không giảm), dòng đó có `operatingStatus='DECOMMISSIONED'` xác nhận qua `psql` độc lập. DELETE id không tồn tại → **404**. |
| **AUTH-01 — `POST /auth/login` (JWT)** | ✅ **Đã hoàn thành**, verify runtime thật | `backend/src/modules/auth/auth.controller.ts`, `auth.service.ts`, `jwt.strategy.ts`, `jwt-auth.guard.ts`. So `bcrypt.compare` với `passwordHash`, trả JWT (`JWT_EXPIRES_IN=8h`, không có refresh token - quyết định 2026-08-10). Sai email/sai password đều trả **401** cùng 1 message `"Email hoặc mật khẩu không đúng"` (không lộ email có tồn tại hay không). `main.ts` (`assertRequiredEnv`) chặn app khởi động nếu thiếu `JWT_SECRET`/`CREDENTIALS_ENCRYPTION_KEY` — verify thật bằng cách ẩn hẳn file `.env`: app exit code 1, không mở cổng. |
| **AUTH-02 — RolesGuard theo 4 role** | ✅ **Đã hoàn thành**, verify runtime thật | `backend/src/modules/auth/roles.guard.ts` + `roles.decorator.ts` (`@Roles(...)`). Áp vào `assets.controller.ts` và `sites.controller.ts`: mọi `GET` chỉ cần đăng nhập; `POST`/`PATCH /assets` yêu cầu `IT_ADMIN`\|`WAREHOUSE_MANAGER`; `DELETE /assets` (soft-delete) chỉ `IT_ADMIN` (bảng phân quyền đã duyệt 2026-08-10). Verify thật qua curl: role `STAFF` gọi `POST /assets` → **403**; role `IT_ADMIN` → **201**. |
| Seed tài khoản `IT_ADMIN` đầu tiên | ✅ Đã chạy | `backend/prisma/seed.ts` (`seedFirstAdmin`) — chỉ chạy nếu bảng `users` rỗng, đọc `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` từ env, **throw lỗi và dừng nếu thiếu** (không tự sinh password ngẫu nhiên). Query thật xác nhận: 1 user `admin@quanly-ccdc.local`, role `IT_ADMIN`, `active=true`. Re-chạy seed lần 2 xác nhận skip đúng (không tạo trùng). |
| Module `assets` — 7 route, **có JWT + RBAC guard** | ✅ Có code, ✅ Đã test runtime thật đầy đủ (không token → 401, sai role → 403, đúng role → 200/201) | `backend/src/modules/assets/assets.controller.ts`: `GET /assets`, `GET /assets/expiring-soon`, `GET /assets/by-tag/:assetTag`, `GET /assets/:id`, `POST /assets`, `PATCH /assets/:id`, `DELETE /assets/:id` (soft delete) |
| Module `sites` — 2 route, **có JWT guard** | ✅ Có code, ✅ Test runtime thật (không token → 401, có token → 200) | `backend/src/modules/sites/sites.controller.ts`: `GET /sites`, `GET /sites/:id` |
| **USER-01 — CRUD tài khoản (`/users`)** | ✅ **Đã hoàn thành**, verify runtime thật đầy đủ | `backend/src/modules/users/`: `users.controller.ts` (6 route: `GET/POST /users`, `GET/PATCH/DELETE /users/:id`, `PATCH /users/:id/password`), `users.service.ts`, `dto/{create-user,update-user,change-password}.dto.ts`. Toàn bộ chỉ `IT_ADMIN` trừ `PATCH /:id/password` (mọi role đăng nhập được, nhưng đổi hộ người khác vẫn chặn ở tầng service nếu không phải IT_ADMIN — hàng rào 2 lớp giống `CredentialsService`). `passwordHash` **không bao giờ** trả về API (dùng Prisma `select` tường minh loại field, xác nhận thật qua response JSON không có key `passwordHash`). bcrypt 10 rounds — khớp `seed.ts`. Soft-delete (`active=false`), chặn tự vô hiệu hóa chính mình. Verify thật qua curl: không token → 401; STAFF tạo user → 403; IT_ADMIN tạo `WAREHOUSE_MANAGER` → 201 không lộ `passwordHash`; login user vừa tạo → thành công; user đó gọi `POST /assets` → 201 (đóng rủi ro #6 cũ); tự đổi password sai `oldPassword` → 401; IT_ADMIN tự xóa mình → 403; IT_ADMIN xóa user khác → 200 soft-delete đúng (`active=false`, không mất dòng). Dữ liệu test đã dọn sạch (`users`=1, `it_assets`=359). |
| **OPS-03 — Import Employee từ Book2.xlsx** | ✅ **Đã hoàn thành** | Script: `backend/prisma/import-book2.ts` (dry-run mặc định, `--commit` để ghi thật, cùng pattern `import-book1.ts`). Nguồn chỉ có **4 dòng** dữ liệu thật (không phải hàng trăm) — audit đầy đủ trước khi code: 0 null, `Mã HRM` unique 4/4, `Mã BC`/`Mã BĐX` khớp 100% với `Site.code`/`Site.wardCode` đã có (site `533140`). `Employee` không có `siteId` trực tiếp — liên kết qua `Employee.departmentId → Department.siteId`; script tự tạo 1 `Department` đại diện cho mỗi Site distinct tham chiếu. Kết quả thật (query `psql` độc lập): `departments`=1, `employees`=4, join `employees→departments→sites` đúng `site_code='533140'` cho cả 4 dòng. |
| **ASSIGN-01 — Cấp phát/thu hồi thiết bị (`/assignments`) + `/employees` (đọc)** | ✅ **Đã hoàn thành**, verify runtime thật đầy đủ. **⚠️ 2 quyết định nghiệp vụ đã ĐỔI (2026-08-11), ghi đè quyết định tạm ban đầu** — xem chi tiết bên dưới | `backend/src/modules/assignments/` (`assignments.controller.ts`: `GET /assignments`, `GET /assignments/:id`, `POST /assignments`, `PATCH /assignments/:id/return`; `assignments.service.ts`; `dto/{create-assignment,return-assignment}.dto.ts`), `backend/src/modules/employees/` (`GET /employees`, `GET /employees/:id`, đọc, mọi role đã đăng nhập). `POST`/`PATCH .../return` chỉ `IT_ADMIN`. **Quyết định đã đổi sau khi review lại với người dùng thật:** (1) **CHẶN** nhiều assignment `ACTIVE` cùng lúc trên 1 asset — 409 Conflict, nêu rõ id assignment cũ đang ACTIVE; (2) **TỰ ĐỘNG đồng bộ `ItAsset.operatingStatus`**: cấp phát → `PRODUCTION`, thu hồi → `IN_STOCK` **chỉ khi** operatingStatus lúc thu hồi **vẫn đúng là `PRODUCTION`** (chưa bị admin sửa tay) — nếu admin đã tự đổi sang `MAINTENANCE`/`FAULTY`/khác trong lúc đang cấp phát thì **tôn trọng, không ghi đè** (admin vẫn có toàn quyền sửa tay qua `PATCH /assets/:id`, field không bị khóa). Chặn cấp phát asset `DECOMMISSIONED` hoặc đang `MAINTENANCE`/`FAULTY` (400). Cả 2 thao tác (assignment + asset) dùng Prisma interactive `$transaction` — không bao giờ lệch nửa chừng. Validate `assetId`/`employeeId`/`departmentId` tồn tại trước khi ghi (400 rõ ràng). Trước khi sửa: audit thật xác nhận **toàn bộ 359 `ItAsset` đều đang `IN_STOCK`** — bật đồng bộ tự động không ghi đè âm thầm dữ liệu cũ nào. Verify runtime thật (curl, không suy đoán): cấp phát asset IN_STOCK → 201, `operatingStatus`→`PRODUCTION` (xác nhận độc lập qua `GET /assets/:id`); cấp phát lại chính asset đó lần 2 (đang ACTIVE) → **409**; thu hồi → `operatingStatus` tự về `IN_STOCK` (xác nhận độc lập); **case quan trọng nhất**: cấp phát asset khác, admin tự tay `PATCH /assets/:id` đổi `operatingStatus`→`MAINTENANCE` trong lúc đang ACTIVE, rồi thu hồi → **`operatingStatus` giữ nguyên `MAINTENANCE`, không bị ghi đè về `IN_STOCK`** (xác nhận độc lập qua `GET /assets/:id` sau thu hồi); cấp phát asset đã `DECOMMISSIONED` → 400; cấp phát asset đang `MAINTENANCE` → 400. Toàn bộ dữ liệu test đã dọn sạch, khôi phục đúng baseline gốc (`SELECT operatingStatus, count(*) GROUP BY` → 359 `IN_STOCK`, `enduser_assignments`=0). |
| **MAINT-01 — Lịch sử bảo trì (`/assets/:assetId/maintenance-logs`)** | ✅ **Đã hoàn thành**, verify runtime thật đầy đủ | `backend/src/modules/maintenance/`: `maintenance.controller.ts` (`GET`/`POST /assets/:assetId/maintenance-logs`), `maintenance.service.ts`, `dto/create-maintenance-log.dto.ts`. `POST` chỉ `IT_ADMIN`; `GET` mọi role đã đăng nhập nhưng field `cost` **chỉ `IT_ADMIN`/`ACCOUNTANT` thấy** — role khác vẫn xem đủ log (không nhạy cảm như VPN), chỉ riêng `cost` bị lọc ở tầng service (destructure loại field trước khi trả). Validate `assetId` tồn tại (400 rõ ràng). Verify thật qua curl: không token → 401; STAFF tạo → 403; IT_ADMIN tạo đủ field (`type/description/supportProvider/cost/ticketRef`) → 201, xác nhận DB đúng qua `psql` độc lập; `GET` với STAFF → response **không có key `cost`**; `GET` với IT_ADMIN → có đủ `cost`; `assetId` sai (cả GET/POST) → 400. Dữ liệu test đã dọn sạch (`it_maintenance_logs`=0). |
| **LIC-01 — License phần mềm (`/assets/:assetId/licenses`)** | ✅ **Đã hoàn thành**, verify runtime thật đầy đủ | `backend/src/modules/licenses/`: `licenses.controller.ts` (`GET`/`POST /assets/:assetId/licenses`), `licenses.service.ts`, `dto/create-license.dto.ts`. `POST` chỉ `IT_ADMIN`; `GET` mọi role đã đăng nhập, **`licenseKey` không mã hóa** (quyết định 2026-08-11 — license phần mềm không nhạy cảm bằng VPN, khác `NetworkAccessCredential`/CRED-02, lưu plaintext trực tiếp, không dùng `EncryptionService`). Validate `assetId` tồn tại (400 rõ ràng). Verify thật qua curl: không token → 401; STAFF tạo → 403; IT_ADMIN tạo license với `licenseKey` → 201, xác nhận DB lưu **plaintext** (không mã hóa) qua query `psql` độc lập; `GET` với STAFF → thấy đầy đủ `licenseKey` (đúng, không hạn chế); `assetId` sai (cả GET/POST) → 400. Dữ liệu test đã dọn sạch (`software_licenses_link`=0). |
| **CRED-02/03/04 — `POST`/`GET /assets/:assetId/credentials`** | ✅ **Đã hoàn thành**, verify runtime thật đầy đủ | `backend/src/modules/credentials/credentials.controller.ts` (mới), `credentials.module.ts` (đã nối controller + import `AuthModule`), `dto/upsert-credentials.dto.ts` (mới). Cả 2 route `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles(UserRole.IT_ADMIN)` — 2 lớp bảo vệ (route-level RolesGuard + `assertIsAdmin()` ở tầng service, có sẵn từ trước). Verify thật qua curl: không token → **401**; token STAFF (cả POST và GET) → **403**; token IT_ADMIN `POST` → **201**, xác nhận DB lưu **mã hóa** (không plaintext) qua query `psql` độc lập; token IT_ADMIN `GET` → **200**, giải mã đúng 100% đối chiếu với giá trị gốc (verify bằng so sánh chuỗi trong script test, không paste plaintext ra chat/log). Dữ liệu test đã dọn sạch sau verify (`network_access_credentials`=0, `users`=1). |
| Mã hóa AES-256-GCM | ✅ Có code, chưa có unit test | `backend/src/common/crypto/encryption.service.ts` |
| **Module `auth` (JWT/RolesGuard)** | ✅ **Đã hoàn thành** | `backend/src/modules/auth/`: `auth.module.ts`, `auth.controller.ts`, `auth.service.ts`, `jwt.strategy.ts`, `jwt-auth.guard.ts`, `roles.guard.ts`, `roles.decorator.ts`, `dto/login.dto.ts`. Import vào `app.module.ts`. |
| Toàn bộ model DB đã có module/route thật | ✅ Không còn model nào "chỉ tồn tại ở tầng schema" | Xem MAINT-01 (`MaintenanceLog`), LIC-01 (`SoftwareLicenseLink`), USER-01 (`User`), ASSIGN-01/OPS-03 (`EndUserAssignment`/`Employee`/`Department`) phía trên — đây là 2 module backend cuối cùng còn thiếu route, giờ đã xong. |
| **AC-01 — `GET /asset-categories`** | ✅ **Đã hoàn thành**, verify runtime thật | `backend/src/modules/asset-categories/`: `asset-categories.controller.ts` (1 route, `JwtAuthGuard`+`RolesGuard`, không `@Roles(...)` — mọi role đăng nhập được, cùng pattern `SitesController`), `asset-categories.service.ts` (`findMany` sắp `orderBy name`), `asset-categories.module.ts` (import `AuthModule`, đăng ký ở `app.module.ts`). Phát sinh từ FE-04: trước phase này **không có route nào** trả danh sách `AssetCategory` (chỉ có seed 29 category, không route đọc) — chặn dropdown "Loại máy" ở form tạo/sửa asset, quyết định mở rộng phase đã được duyệt 2026-08-11 thay vì hoãn/dùng input thô. Verify thật qua browser: dropdown "Loại máy" trong modal Tạo/Sửa hiện đủ 29 category lấy từ API thật (xác nhận qua `document.querySelectorAll('select')[0].options`, không phải danh sách cứng trong code). |
| **FE-04 — Frontend CRUD cho assets (tạo mới, sửa, thanh lý)** | ✅ **Đã hoàn thành**, verify runtime thật đầy đủ | `frontend/src/pages/AssetsPage.tsx` (nút "+ Thêm thiết bị" cạnh tiêu đề, chỉ hiện nếu `role ∈ {IT_ADMIN, WAREHOUSE_MANAGER}`; nút "Sửa"/"Thanh lý" trong khối chi tiết Nhóm A, "Thanh lý" chỉ `IT_ADMIN` và ẩn khi asset đã `DECOMMISSIONED`; `window.confirm()` trước khi gọi DELETE), `frontend/src/components/AssetFormModal.tsx` (form dùng chung tạo/sửa: `assetTag*/name*/categoryId*` + `siteId/manufacturer/model/serialNumber/ipAddress/macAddress/operatingStatus/ownershipStatus`, cố ý bỏ `specs/warrantyExpiresAt/supportExpiresAt/purchaseCost` khỏi form phase này — quyết định đã duyệt 2026-08-11). Cập nhật state tại chỗ sau tạo/sửa/xóa (không gọi lại toàn bộ `GET /assets`, đúng thiết kế đã duyệt). **Bug thật phát hiện + sửa trong lúc verify runtime**: response của `POST`/`PATCH`/`DELETE /assets` không `include` `category`/`site` lồng (`assets.service.ts` — chỉ `findOne`/`findAll` mới include) — chèn thẳng response vào state làm React crash trắng trang (`Cannot read properties of undefined (reading 'name')`, xác nhận qua console lỗi thật). Sửa: sau mỗi thao tác thành công, gọi thêm `GET /assets/:id` (đã có include đầy đủ) để lấy đúng 1 dòng hoàn chỉnh trước khi merge vào state — không phải fix bằng cách gọi lại toàn bộ `GET /assets`. Verify thật qua browser thật (Vite dev server + Nest dev server chạy song song, không phải suy đoán): (a) IT_ADMIN thấy nút "+ Thêm thiết bị", tạo asset test (`assetTag=FE04-TEST-001`) → `POST /assets` 201, xuất hiện đúng trong bảng không cần reload; (b) sửa 1 field (`serialNumber`) → `PATCH` 200 → cập nhật đúng tại chỗ, không crash; (c) Thanh lý → `DELETE` 200, `operatingStatus` đổi `DECOMMISSIONED`, dòng **vẫn còn** trong bảng (359/359 → không đổi tổng, đúng thiết kế soft-delete AST-05), nút "Thanh lý" tự ẩn sau đó; (d) tạo user `STAFF` tạm qua `POST /users` (IT_ADMIN, xoá ngay sau test) → đăng nhập STAFF → xác nhận **không** thấy nút "+ Thêm thiết bị", mở chi tiết asset bất kỳ → **không** thấy nút Sửa/Thanh lý, **không** thấy khối VPN/FortiClient (giữ đúng hành vi CRED-04 cũ); (e) tạo asset với `assetTag` trùng → hiện đúng message backend `assetTag "..." đã tồn tại (asset id: ...)` (409), modal không đóng, trang không crash. Dữ liệu test đã dọn: asset test xoá cứng qua Prisma script trực tiếp DB (không có route hard-delete theo thiết kế AST-05 — soft-delete only), user STAFF test xoá cứng tương tự; baseline khôi phục đúng (`it_assets`=359, `users`=1, xác nhận qua script độc lập). **Phát hiện lệch phụ trong lúc verify (không thuộc phạm vi FE-04, đã tự sửa để verify được)**: `SEED_ADMIN_PASSWORD` trong `.env` không khớp `passwordHash` thật của `admin@quanly-ccdc.local` trong DB (login curl → 401 trước khi sửa) — đã reset lại `passwordHash` khớp đúng giá trị hiện có trong `.env` (bcrypt 10 rounds, cùng pattern `users.service.ts`), xác nhận qua đăng nhập lại thành công cả curl lẫn browser thật. Không rõ nguyên nhân lệch (đổi tay hay sửa `.env` sau seed) — ghi nhận, không suy đoán thêm. `vite build`/`nest build` đều pass 0 lỗi trước và sau khi verify. |
| Frontend — routing skeleton | ⚠️ `/assets` có trang thật, `/`, `/sites`, `/scan` vẫn placeholder | `frontend/src/App.tsx` |
| **FE-02 — Trang danh sách thiết bị (`/assets`)** | ✅ **Đã sắp xếp lại đúng thứ tự cột Book1.xlsx gốc (2026-08-10)** | `frontend/src/pages/AssetsPage.tsx` đính JWT (FE-03). Bảng chính giờ theo đúng 14 cột theo thứ tự Excel gốc: `assetTag, Mã BĐT/TP, Tên BĐT/TP, Mã MBC, Tên bưu cục, Mã BĐX, Tên Bưu điện xã, Loại, IP, Tên máy (cắt tiền tố từ notes), Địa chỉ MAC, Loại máy, Hãng, Model`. Chi tiết mở rộng chia 2 nhóm: **Nhóm A** (mọi role) = Serial, HĐH, CPU, RAM, Ổ cứng, Người sử dụng, Mã BĐKV, Tên BĐKV, Bưu điện xã trung tâm, Địa chỉ chi tiết; **Nhóm B (CRED-04, chỉ IT_ADMIN)** = User/Password VPN + FortiClient, lazy-load qua `GET /assets/:id/credentials` chỉ khi mở rộng đúng dòng đó (không gọi trước cho 359 dòng), role khác **ẩn hoàn toàn** Nhóm B (không tiêu đề, không request). Cột `operatingStatus` ("Tình trạng") và `ownershipStatus` ("Sở hữu") cố ý bỏ khỏi UI (không thuộc danh sách cột đã chốt) — xem nợ kỹ thuật #9. Verify thật qua browser (không suy đoán): STAFF mở chi tiết → Nhóm B ẩn hoàn toàn + 0 request `/credentials` mới; IT_ADMIN + asset có credential → hiện đúng data giải mã; IT_ADMIN + asset chưa có → hiện "Chưa cấu hình VPN/FortiClient" (xử lý đúng trường hợp body rỗng từ BE, không phải JSON `null`); thứ tự cột khớp 100%. `vite build` 0 lỗi, console 0 lỗi. |
| **FE-03 — Trang đăng nhập + đính JWT + RequireAuth** | ✅ **Đã hoàn thành**, verify runtime thật đầy đủ (5 kịch bản a-e) | `frontend/src/pages/LoginPage.tsx` (form email/password, gọi `POST /auth/login`, lưu session, redirect `/assets`); `frontend/src/lib/auth.ts` (`getToken`/`getUser`/`setSession`/`clearSession`/`isAuthenticated` - dùng localStorage, xem nợ kỹ thuật #8 bên dưới); `frontend/src/components/RequireAuth.tsx` (check token trước khi render, redirect `/login` ngay nếu thiếu - không đợi 401); `AssetsPage.tsx` sửa: đính JWT vào `fetch`, 401 → `clearSession()` + redirect `/login`; hiện `fullName (role)` + nút "Đăng xuất" ở góc trên. `App.tsx` thêm route `/login`, bọc `/assets` bằng `<RequireAuth>`. Verify thật qua browser thật (không phải curl): (a) mở `/assets` chưa đăng nhập → redirect `/login` ngay, không nháy nội dung; (b) sai password → hiện đúng message backend; (c) đúng (IT_ADMIN đã seed) → redirect `/assets`, 359/359 thiết bị, hiện đúng "IT Admin (seed) (IT_ADMIN)"; (d) đăng xuất → token bị xóa thật (`localStorage` = null), mở lại `/assets` bằng URL → redirect `/login` lại; (e) mở `/login` khi đã có token → tự redirect `/assets`. Console 0 lỗi (tab sạch). `vite build` pass 0 lỗi. |
| **Bug có sẵn từ scaffold, phát hiện+sửa ở FE-02** | ✅ Đã sửa | `frontend/src/main.tsx` thiếu `<BrowserRouter>` — mọi route (không riêng `/assets`) crash ngay khi mount trước khi sửa. Đã bọc `<App />` trong `<BrowserRouter>`. |
| Frontend — Rack visualizer tương tác | ❌ Chưa code | Không tìm thấy component nào tên rack/visualizer trong `frontend/src/` |
| Frontend — QR scan mobile-web | ❌ Chưa code | Route `/scan` chỉ là `PlaceholderPage`, không có logic camera/decode |
| Offboarding/decommission state machine | ❌ Chưa code | `EndUserAssignment.status` enum có `OFFBOARDING`(schema.prisma:189-193) nhưng không có service/controller nào implement luồng thu hồi → backup → wipe → license → kho |
| Build backend (`nest build`) | ✅ Pass, 0 lỗi | Chạy thật trong phiên trước; log không có output lỗi sau khi sửa `assets.service.ts` (cast `Prisma.InputJsonValue`) |
| Build frontend (`vite build`) | ✅ Pass, 0 lỗi | Output thật: `dist/index.html`, `dist/assets/index-BTeJBiAA.js` (160.89 kB) |
| Server chạy được qua HTTP (runtime thật) | ✅ **Đã kết luận được** (từ FE-02, 2026-08-10) | `npm run start:dev` chạy thật nhiều phiên liên tiếp (FE-02, AST-04/05, AUTH-01/02), verify bằng curl + browser thật, không còn là "chưa xác nhận" |

## Rủi ro / lệch hướng đã ghi nhận

1. **✅ RESOLVED — `credentials` module giờ có route thật (CRED-02/03/04, 2026-08-10)**, chỉ `IT_ADMIN`, verify runtime đầy đủ (401/403/201/200 đúng theo từng vai trò, mã hóa DB xác nhận). Không còn là rủi ro treo.
2. **4 cột User VPN / Password VPN / User FortiClient / Password FortiClient trong Book1.xlsx bị bỏ qua hoàn toàn khi import (quyết định có chủ đích, đã duyệt 2026-08-10)** — `import-book1.ts` không đọc, không lưu 4 cột này dưới bất kỳ hình thức nào (xác nhận thật: `network_access_credentials` = 0 dòng). Dữ liệu VPN/FortiClient thật của 359 thiết bị **hiện không tồn tại ở đâu trong hệ thống mới**.
3. **Cột "Loại" (GD3/VHX/GD1/VP/GD2/PH2/PH1) và các field site khác vẫn cần dọn tay** — `Site.pointType`, `wardCode`, `wardName`, `centralWardName` được import nguyên trạng từ Excel, chưa chuẩn hóa/đối chiếu nghiệp vụ. Riêng site `531130` (VP BĐ Thành Phố Huế) có `wardName = NULL` do xung đột dữ liệu thật.
4. **73/359 `ItAsset` đang mang category `UNCLASSIFIED`** (thiếu "Loại máy" hoặc ghi "Không có máy tính" trong Excel gốc) — cần người dùng tự phân loại lại thủ công sau.
5. **✅ RESOLVED HOÀN TOÀN — `/assets` từng public không auth (FE-02), rồi vỡ 401 (AUTH-01/02), giờ đã có đăng nhập đầy đủ (FE-03, 2026-08-10).** Backend có `JwtAuthGuard`/`RolesGuard`, frontend có `LoginPage` + `RequireAuth` + đính JWT vào request. Cả 5 kịch bản runtime (chưa đăng nhập/sai password/đúng/đăng xuất/đã có token) đã verify thật qua browser. Không còn là rủi ro treo.
6. **✅ RESOLVED — nhánh `WAREHOUSE_MANAGER` giờ đã verify runtime thật (USER-01, 2026-08-11)**. Tạo tài khoản `WAREHOUSE_MANAGER` thật qua `POST /users` (IT_ADMIN), đăng nhập thành công, gọi `POST /assets` → **201** — xác nhận đúng như bảng phân quyền AUTH-02 đã duyệt. Tài khoản test đã bị xóa (soft-delete) sau khi verify.
7. **Chưa có route đăng xuất/thu hồi token (logout/blacklist) ở backend** — nút "Đăng xuất" ở FE (FE-03) chỉ xóa session cục bộ (`clearSession()`), JWT thật vẫn còn hiệu lực ở backend tới khi hết hạn (8h) nếu ai đó vẫn giữ token đó dùng lại. Đây là hạn chế đã biết của thiết kế "chỉ access token, không refresh token, không blacklist" đã duyệt, không phải lỗi.
8. **⚠️ Nợ kỹ thuật mới (FE-03, 2026-08-10) — JWT lưu ở `localStorage` (`frontend/src/lib/auth.ts`), không phải httpOnly cookie.** Quyết định có chủ đích cho giai đoạn dev/nội bộ hiện tại (ưu tiên không phải đăng nhập lại mỗi lần F5). Rủi ro: nếu sau này có lỗ hổng cho phép inject script (XSS) vào trang, script đó đọc được token trực tiếp từ `localStorage`. **Cần xem lại, chuyển sang httpOnly cookie, trước khi deploy ra môi trường có người ngoài truy cập được.**
9. **`operatingStatus` ("Tình trạng") và `ownershipStatus` ("Sở hữu") không còn hiển thị ở đâu trên `/assets` (quyết định có chủ đích, 2026-08-10)** — khi sắp xếp lại UI theo đúng thứ tự cột Book1.xlsx gốc, 2 field này (vốn tồn tại thật trong `ItAsset`, không phải từ Excel) bị loại khỏi cả bảng chính lẫn "Chi tiết" vì không nằm trong danh sách cột đã chốt. Dữ liệu vẫn còn nguyên trong DB (API vẫn trả về), chỉ là không có nơi hiển thị nào ở FE hiện tại — cần quyết định lại nếu muốn xem 2 field này qua UI. "Ngày cấp" và "Tình trạng hoạt động" (2 cột gốc trong Excel) vẫn như cũ: không có nơi lưu trong DB, không hiển thị.
